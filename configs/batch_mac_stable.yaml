# Mac-stable batch overlay: less GPU churn; Match 3 ball still map+hold on raw dets.
detection:
  enhance_ball: false
  use_sahi: false
  use_kalman: false

checkpoint:
  interval_frames: 150

team_assignment:
  # Empty = match-root / run_dir resolve (see docs/product/KIT_REF.md)
  kit_centroids_path: ""
