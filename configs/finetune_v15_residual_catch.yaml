# v15 residual specialty — Catch RTX 5090 (resume v14 @275 → 285, ~10 epochs @560).
model:
  architecture: detr
  backbone: resnet50
  num_classes: 1
  pretrained: true
  hidden_dim: 256
  nheads: 8
  num_encoder_layers: 6
  num_decoder_layers: 6
  rfdetr_size: base
  remap_mscoco_category: false

dataset:
  coco_train_path: /home/catch/soccer_coach_cv/datasets/ball_finetune_v15_residual/train
  coco_val_path: /home/catch/soccer_coach_cv/datasets/ball_finetune_v15_residual/valid
  yolo_train_path: /home/catch/soccer_coach_cv/datasets/ball_finetune_v15_residual/train
  yolo_val_path: /home/catch/soccer_coach_cv/datasets/ball_finetune_v15_residual/valid
  ball_class_id: 1
  category_name: ball
  category_id: 0
  use_combined_dataset: false
  pin_memory: true
  prefetch_factor: 1
  persistent_workers: false

training:
  epochs: 285
  batch_size: 4
  grad_accum_steps: 4
  learning_rate: 0.00004
  encoder_learning_rate: 0.000025
  weight_decay: 0.0001
  resolution: 560
  amp: true
  mixed_precision: true
  num_workers: 2
  device: cuda
  multi_scale: false
  expanded_scales: false

augmentation:
  motion_blur: true
  jpeg_compression: true
  horizontal_flip: true

output:
  output_dir: /home/catch/soccer_coach_cv/models
  save_every_epochs: 1

checkpoint:
  resume_from: /home/catch/soccer_coach_cv/models/v14_residual_snaps/post_train/checkpoint.pth
  save_dir: models/v15_residual_snaps
