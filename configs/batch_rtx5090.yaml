# RTX 5090 batch overlay — throughput-first; Phase 1 emit gate unchanged.
detection:
  enhance_ball: false
  use_sahi: false
  use_kalman: false

checkpoint:
  interval_frames: 600

team_assignment:
  golden_batch_frames: 600
  # Prefer empty + script seeding (KIT_REF / match-root team_centroids.json). Optional override:
  # kit_centroids_path: data/output/match_4_5min/team_centroids.json
  kit_centroids_path: ""
